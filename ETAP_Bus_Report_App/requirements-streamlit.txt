# Requirements for deploying streamlit_app.py (e.g. on Streamlit Community
# Cloud). Deliberately excludes PySide6 and pyinstaller - those are for the
# desktop app (ui.py / build.spec) only, and PySide6 in particular can fail
# to install in slim cloud containers that lack system Qt/OpenGL libraries.
#
# In Streamlit Cloud: Advanced settings -> Requirements file ->
# requirements-streamlit.txt   (or just rename this to requirements.txt in
# your deployment branch/fork if the platform doesn't let you pick a name).

streamlit>=1.30
PyMuPDF>=1.24
python-docx>=1.1

# Optional - only needed for OCR fallback on scanned/rasterized PDFs.
pytesseract>=0.3
Pillow>=10.0
