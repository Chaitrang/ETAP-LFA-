PySide6
PyMuPDF
python-docx
pdfplumber
pandas

# Web deployment (streamlit_app.py) - not needed for the desktop GUI (ui.py)
streamlit

# Optional - only needed for OCR fallback on scanned/rasterized PDFs.
# The app runs fine without these; scanned-PDF support is simply skipped
# if they are not installed.
pytesseract
Pillow

# Packaging (desktop build only - not needed for Streamlit deployment)
pyinstaller
